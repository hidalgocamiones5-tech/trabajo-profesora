from django.apps import AppConfig

class RagAdminConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'rag_admin'
    verbose_name = '5. Inteligencia Artificial & Motor RAG'
